# 批 2：acp-events-session — peri-acp 事件与 session 路径改经 Controller

**状态**：active（修复审批 REJECT 后迭代中）
**标签**：`3.0-refactor`、`acp-events-session`、`batch-2`
**类型**：重构
**创建日期**：2026-08-05
**来源**：`spec/issues/2026-08-05-peri-3.0-refactor-prd.md`（伞形 PRD 决策 7/8）+ `docs/design/architecture.md` v3.9（§0/§6/§9）
**最后核查**：2026-08-22

## 最新情况（2026-08-22）

波 1-4 的历史交付保持；L5 已把 executor 本体迁到 `peri-agent/src/session/exec/`，ACP `session/executor.rs` 仅保留 re-export 薄桥。当前事件边界进一步明确：`BgRegistryEvent` 仍是无标准 `SessionUpdate` 的私有功能载体，TUI 私有通知按 capability 门控，Hub/Web 只接收 `event/activity.rs` 的版本化 allowlist 安全投影；`LlmRetrying` 是可展示的重试进度，真正 fatal 才经 `PromptResult.failure` 在 ACP 边界转成 JSON-RPC server error，cancel/interrupted/max iterations 仍为成功 `PromptResponse`。Langfuse bridge/tracer 实现已在 Controller，ACP forwarder 仅保留协议化前分支接线点。原审批记录与逐处裁定表保留为历史快照；import exemptions/Cargo 依赖是否已完全按 L5 收紧仍需单独验收，不据本次文档核查宣告关闭。

## 任务定义

**本批目标**：peri-acp 不再直接依赖 peri-agent / peri-middlewares / peri-lsp / peri-workflow 的业务面（仅允许依赖 peri-controller / peri-acp-types；经装配注入的类型除外）。

**验收标准（分波 2）**：
- executor 编排薄壳 / `run_session_loop` 调用 → `Controller.run_session`；AgentEventHandler / 事件订阅 → Controller 事件弹出订阅
- AgentCancellationToken / CronOwner / goal / inbox / interaction 等直接依赖 → Controller 透传接口或装配注入
- 事件三层化收尾：v1 兼容映射保留在 ACP 协议面（peri-acp-types），发射点统一经 Controller
- 验收：peri-acp/src 中不再出现 `use peri_agent::`（测试文件允许但尽量收敛；build_v2_subagent/background 等已迁出后不得回引）

## 审批结论（首次，REJECT）

审批人核验后 REJECT，三个核心未达标项：

1. **「use peri_agent::」系全路径引用规避，实质依赖未消除**：118 处 `peri_agent::` 全路径引用（非测试代码），含直接调用业务面（`run_react_loop` / `SessionFactory::spawn_subagent` / `AgentModelBridge::new` / `TaskManager::new()` 默认回退 / `build_middleware_chain`）；Cargo.toml 仍依赖 4 个业务 crate。
2. **三条事件旁路绕过 Controller**：`executor_helpers.rs:655`（Phase 2 bg event pump）、`executor.rs:700-709`（bg 命令事件泵）、`executor.rs:721-785`（Registry 事件泵）直推 `EventSink`，缺 Runtime 补打身份（违 §9 事件契约）。
3. **executor 编排薄壳未达成**：`run_session_loop` 完整编排本体仍在 ACP `host/exec/`。

**审批修复点**：
① 三条事件旁路改经 `Controller::publish_event`（带 `UnstampedEvent` 身份），消费端统一从 `Controller::subscribe` 订阅；
② 118 处全路径引用逐处裁定：直接调用业务函数应收窄为经注入的 trait/句柄，**或与任务规划者明确「过渡宿主豁免至 L5」并写回 active spec**；
③ Cargo.toml 的 4 个业务 crate 依赖随收窄移除或**列入显式豁免清单**。

## 修复内容（本轮）

### 修复点 ①：三条事件旁路 → Controller（已完成）

| 旁路 | 原实现 | 修复后 |
| --- | --- | --- |
| Phase 2 bg event pump（`executor_helpers.rs`） | `bg_event_rx` → 直推 `EventSink::push_event` | 发射经 `Controller::publish_event`（身份：v2 循环 turn_id / 主 agent_id）；消费端为主 pump（已订阅 Controller） |
| bg 命令事件泵（`executor.rs`） | `bg_event_rx_for_cmd` → 直推 `push_event`/`push_done` | 发射经 `Controller::publish_event`（agent_id 打 [`BG_CMD_EVENT_AGENT`] 标记）；消费端本泵自建订阅，按 session_id + 标记过滤（防与主 pump 双推） |
| Registry 事件泵（`executor.rs`） | `BgRegistryEvent` → 直推 `push_unstable_event` | 发射经 `Controller::publish_event`（`ExecutorEvent::BgRegistryEvent` 新变体载体，身份空串降级）；消费端本泵自建订阅，解包映射回 `bg-task-*` unstable 事件（TUI 协议面不变） |

配套改动：
- `peri-acp-types/src/event.rs`：新增 `ExecutorEvent::BgRegistryEvent(crate::tasks::BgRegistryEvent)` 变体（事件链载体；serde 新增 tag 向后兼容）
- `peri-acp/src/event/mapper.rs`：穷尽匹配新增分支（无 SessionUpdate 输出）；`variant_coverage_test.rs` 同步登记
- `peri-controller/src/langfuse/bridge.rs`：`from_executor_event` 穷尽匹配新增分支（None）
- `build_and_execute_agent_v2` / `build_and_execute_agent` 签名移除不再使用的 `event_sink` / `effective_context_window` 参数

### 修复点 ②：118 处 `peri_agent::` 引用逐处裁定（见下「逐处裁定表」）

收窄结论：**不新增任何收窄动作**——经裁定，审批点名的直接调用均属「执行本体 / 装配面」，按「过渡宿主豁免至 L5」处理（写回本 spec）。豁免依据：L5 子 issue（`2026-08-05-3.0-l5-executor-split.md`）明确「9-phase loop（RCRA）→ Agent 层 session 运行器；build_* → Agent 层 session 工厂；ACP 只留协议化薄壳」，执行本体物理迁出是 L5 交付物；审批人已确认「物理迁出属 L5」（仅拒绝无记录的规避）。

### 修复点 ③：Cargo.toml 依赖豁免清单（见下）

## 逐处裁定表（非测试代码 `peri_agent::` 引用，98 处）

### A. 执行本体（`host/exec/` 过渡宿主，豁免至 L5）

L5 迁出后引用随物理迁移自然消失。逐项裁定：

| 引用（审批点名） | 位置 | 裁定 |
| --- | --- | --- |
| `run_react_loop` 调用 | `executor_helpers.rs:812`、`workflow_agent.rs:593` | 豁免：RCRA loop 本体驱动，L5 迁 Agent 层 session 运行器（§2/§21） |
| `LoopResult` 匹配 | `executor_helpers.rs:857-900`、`workflow_agent.rs:596-662` | 豁免：同上（循环返回类型） |
| `SessionFactory::spawn_subagent` | `executor_helpers.rs:171`（DefaultBgForkSpawner） | 豁免：已经注入的 `BgForkSpawner` trait 发起（命令层零引用）；默认实现是 /bg 发起执行本体，L5 迁入 Agent 层 |
| `AgentModelBridge::new/from_arc` | `executor_helpers.rs:104`、`executor.rs:1310`、`stage_builder.rs:269,411`、`workflow_agent.rs:500` | 豁免：ReactLLM 适配构造（LLM 装配执行本体）；llm 工厂已有注入闭包模式，构造点随 L5 迁入 session 工厂 |
| `TaskManager::new()` 默认回退 | `stage_builder.rs:306` | 豁免：`AssemblyContext.task_manager` 为必填 `Arc`（`peri-middlewares::assembly` 契约），SubAgentMiddleware 依赖其注册子 agent（行为契约）；回退构造随 L5 迁入 Agent 层 session 工厂（代码注释已更新说明） |
| `build_middleware_chain` | `stage_builder.rs:344` | 豁免：已经注入的 `MiddlewareChainAssembler` trait（`&ProductionChainAssembler`）调用，唯一触发点与链序事实源在 Agent 层 session 工厂（ARC-MIDDLEWARE-001）；调用点随 L5 迁出 |
| `TaskManager` downcast | `executor_helpers.rs:135-139,616-621` | 豁免：经装配注入的 `Arc<dyn TaskManager>` downcast 还原（「经装配注入的类型除外」），随 L5 迁出 |
| `SubagentHost` / `SubagentSpawnConfig` 等 | `executor_helpers.rs:148-215` | 豁免：/bg 发起执行本体，L5 迁出 |
| `StageContext`/`StageContextBuilder`/`SharedToolMap` | `stage_builder.rs:482-945` | 豁免：循环上下文装配，L5 迁出 |
| `AgentInput`/`AgentState` | `executor.rs:837,852,955,969`、`executor_helpers.rs:586,836` | 豁免：执行本体类型签名，L5 迁出 |
| `MiddlewareChain`/`Middleware` trait/`BaseTool`/`ToolContext` | `stage_builder.rs:66,69,618`、`workflow_agent.rs:368-487`、`executor.rs:332`、`executor_helpers.rs:119-127,187` | 豁免：执行装配类型，L5 迁出 |
| `ContextBudget` | `stage_builder.rs:325`、`workflow_agent.rs:321-325` | 豁免：循环上下文预算类型，L5 迁出 |
| `compact_v2` 系列（`run_compact`/`CompactResult`/`ContextPressure`/`CONTINUATION_HINT`/`extract_*`） | `compact_pipeline.rs:290-351` | 豁免：/compact 命令 v2 执行体，L5 迁出（§15 compact 归 Agent） |
| `MessageTranscript` | `executor_helpers.rs:824`、`compact_pipeline.rs:153,282,331` | 豁免：执行本体类型，L5 迁出 |
| `FrozenContext`/`Session` | `executor.rs:157-241`、`stage_builder.rs:626-628,941-942`、`workflow_agent.rs:686` | 豁免：执行本体类型（ARC-FROZEN-001 数据载体），L5 迁出 |
| `ErrorSuggestRegistry`/`ToolRegistrySnapshot` | `stage_builder.rs:71-73` | 豁免：执行装配类型，L5 迁出 |
| `LangfuseBridgeLike` | `stage_builder.rs:383,768` | 豁免：观测桥 trait 引用（§6 旁路），L5 迁出 |
| `ReactLLM` | `executor_helpers.rs:102`、`executor.rs:1309`、`stage_builder.rs:64,230,415`、`workflow_agent.rs:505` | 豁免：LLM 抽象 trait 引用（§1 Model 层适配后），L5 迁出 |
| `DefaultSubagentV2ContextBuilder` 回退 | `workflow_agent.rs:534` | 豁免：注入 trait（`SubagentV2ContextBuilder`）的默认实现回退，L5 迁出后取消回退强制注入（代码注释已更新） |

### B. 装配面 / 类型签名（非 exec 目录，豁免）

| 位置 | 内容 | 裁定 |
| --- | --- | --- |
| `host/assemble.rs:97` | `TaskManager::new()`（装配注入实例构造点） | 豁免：装配面 = ACP Host 部署单元组装，构造业务实现注入（「经装配注入的类型除外」）；L5 后随装配迁 Agent 层 |
| `host/stdio/init.rs:29` | `telemetry::init_tracing`（ACP Host 初始化） | 豁免：宿主装配初始化，随 stdio 宿主归位（PRD 决策 21） |
| `host/mod.rs:109`、`host/prompt.rs:39`、`host/stdio/context.rs:63` | `Arc<dyn BaseTool>` 类型签名（shared_tools） | 豁免：执行上下文类型签名，L5 执行本体迁出后消失 |
| `session/mod.rs` | 注释引用 | 无代码引用 |

## 豁免清单（明确至 L5）

以上 A/B 表全部引用为**过渡宿主豁免项**，物理消除路径 = L5 executor 拆分（历史实施记录由 Git 保留）：
- `host/exec/` 全部执行本体（run_session_loop 编排 / build_and_execute_agent_v2 九 phase / stage_builder 装配 / forwarder / workflow_agent / bg spawner / compact_pipeline）→ 迁入 peri-agent session 工厂/运行器
- 装配面构造点（assemble.rs / stdio init）→ 随 L5 装配归位
- 豁免期内禁止新增业务面直接调用；新代码一律经注入 trait / Controller 接口

## Cargo.toml 显式豁免清单（修复点 ③，波 2 更新）

`peri-acp/Cargo.toml` 的 3 个业务 crate 依赖**列入显式豁免清单**（不随本批移除；随上述 L5 迁出后逐项移除）：

| 依赖 | 豁免理由 |
| --- | --- |
| `peri-agent` | 执行本体（A 表全部）编译期依赖；L5 迁出后移除 |
| `peri-middlewares` | host/exec 执行本体（A 表装配类型与中间件构造）与测试文件引用；L5 迁出后移除 |
| `peri-workflow` | host/exec/workflow_agent.rs（执行本体）与测试文件引用；L5 迁出后移除 |
| ~~`peri-lsp`~~ | **波 2 已移除**：装配面类型（LspServerConfig）波 1 已 move 至 peri-acp-types，peri-acp 代码零引用 |

## 批 2 波 2：装配注入收口（acp-middleware-types）

波 2 完成内容（对应任务「peri-acp middleware/lsp/workflow 类型改经装配注入」）：

- **装配边界收口**：宿主装配点（peri-tui main.rs / launch.rs / cli_print.rs、host/assemble.rs、host/stdio）构造具体实现后 upcast 注入端口；ACP 侧只持 `peri_acp_types` 端口接口（`McpPoolPort` / `CronSchedulerPort` / `ToolSearchPort` / `WorkflowMiddlewarePort` / `SkillsPort` / `PluginManagerPort` / `SettingsHooksPort`）
- **资源类不直接 new**：McpClientPool / CronScheduler / ToolSearchIndex 由宿主构造，经 `HostAssemblyInput` / `StdioContext` / `AcpServerConfig` 注入
- **接口化**：`AcpServerConfig` / `SessionContext` / `StdioContext` 全字段接口化（middlewares/lsp/workflow 类型零 concrete 字段，仅保留 peri-agent 执行本体类型与注入端口）
- **验收门**：新增 `scripts/check-acp-imports.sh` 并挂接 lefthook（`acp-imports`）。校验 peri-acp/src 非测试代码不 import `peri_middlewares` / `peri_lsp` / `peri_workflow`；豁免：`host/exec/`（执行本体，豁免至 L5）与 `*_test.rs` / `tests/`（测试）
- **残留引用**：非测试非豁免引用已清零；剩余引用仅 `host/exec/`（4 文件，A 表豁免）与 5 个测试文件

§0 依赖方向 CI 门：`check-resources-imports.sh`（Resources）+ `check-acp-imports.sh`（ACP，波 2 新增）双门覆盖；L5 完成后随执行本体迁出继续收紧。

## 批 2 波 3：§0 全边依赖门（Seam 3 完整版，ci-gate）

三个旧 check 脚本（`check-resources-imports.sh` / `check-acp-imports.sh` / `check-tui-imports.sh`）**删除**，统一为 `scripts/check-layer-imports.sh` + `scripts/import-exemptions.conf`（豁免清单唯一事实源，每条豁免标注归属收紧任务）：

- **八条边**：TUI（禁入 agent/middlewares/lsp/workflow/resources/model，controller 禁入暂缓随 M-TUI 强制）/ ACP 业务面（middlewares/lsp/workflow）/ ACP→model / Controller / Runtime / Resources / Middlewares / Agent
- **双模式**：每边 use 导入行 + 全路径引用行（`peri_xxx::`）分别校验——全路径模式防「use 规避」复发（批 2 REJECT 教训：118 处 `peri_agent::` 全路径引用）
- **测试文件全局豁免**（`*_test.rs` / `*_test/` / `tests/`）：测试经引用业务 crate 仅验证行为
- **挂接**：lefthook（`layer-imports`）+ CI（`Check §0 layer imports`）各单一条目
- **验收**：当前工作区全边 exit 0；故意违规（TUI `use peri_agent::`、ACP host/ 下 `use peri_middlewares::assembly` 等八边各一）均 exit 1；豁免活性交叉验证 26/26（逐一临时移除后脚本报违规，防僵尸清单）

**裁定写回（ACP→model 边）**：`peri-acp` 非 host/exec 残留 `peri_model` 引用三组——① `provider/mod.rs`（LLM provider 配置/工厂）与 `session/retry_events.rs`（RetryObserver 转发）豁免 + 随 v1 事件退役收敛（v1 任务不处理 retry_events 则单独下沉 `RetryObserver` 至 peri-acp-types）；② `event/mapper.rs`（StopReason 协议映射）豁免 + 收敛至 peri-acp-types 再导出；③ `dispatch/`、`session/agent_pool.rs`、`session/command/` 执行上下文类型签名随 L5 迁出。middlewares→agent「执行面」细则、TUI→controller 禁入为 conf 中 TODO 预留，对应任务（L2 / M-TUI）完成后解除豁免强制。

## 批 2 波 4：v1 ExecutorEvent 中间态退役（v1-retire）

事件发射与消费统一 v2 形态；v1 兼容层仅保留协议序列化面需要的最小映射（在 peri-acp-types）：

- **peri-agent 内部 v1 发射路径退役**：`StreamingContext.event_handler`（v1 `AgentEventHandler`）→ `event_bus`（v2 直发），`model_bridge` 流式增量直接 emit `RenderEvent::TextChunk/ThinkingChunk` + `ObserveEvent::AiReasoningChunk`；`stages/reason.rs` 的 `StreamingEventBridge`（v1→v2 冗余桥）删除；`ReactLLM::inject_event_handler`（默认空实现，middlewares 4 处调用）删除。
- **subagent 生命周期发射统一 v2**：`subagent.rs` 的 v1 `SubagentStarted/Stopped` 直发退役——改为 `build_subagent_start_v2/stop_v2`（ObserveEvent 构造，发射语义单一事实源）+ `forward_subagent_start_v1/stop_v1`（经 `observe_event_to_executor` 同步映射直发父 handler / bg 泵，保持 Started/Stopped 与 BackgroundTaskCompleted 顺序契约；child_agent_id → instance_id 身份透传，C1）。`BgCleanupGuard` 合并为单一 v2 补发（v2 emit + v1 协议化直发同源）。`BackgroundTaskCompleted` 保留为功能载体（bg 泵 push_done 依赖，无 v2 等价物）。
- **保留（协议序列化面）**：`ExecutorEvent` 定义与 `*_event_to_executor` 映射留在 peri-acp-types；`event_sink`/`mapper`/ACP forwarder/retry_events 不变；TUI 已无 v1 消费（AcpEvent 化），wire format 不变。
- **验收**：`cargo test -p peri-agent -p peri-acp -p peri-middlewares -p peri-acp-types` 全绿（agent 604 / acp 360+7 / middlewares 1069 / types 81）；`cargo clippy --workspace --all-targets -- -D warnings` 通过；ARC-EVENT-001 更新为单链路验证。

## 状态变更记录

| 日期 | 从 | 到 | 操作人 | 说明 |
|------|-----|-----|--------|------|
| 2026-08-05 | — | active | agent | 批 2 首次实现（事件三层化/类型化收口/执行发起经 Controller） |
| 2026-08-05 | active | active（修复中） | 审批人 | REJECT：全路径引用规避 / 三条事件旁路 / 编排薄壳；给出修复点 ①②③ |
| 2026-08-05 | active（修复中） | active | agent | 修复点 ① 完成（三条旁路经 Controller）；修复点 ② 逐处裁定完成（豁免写回本 spec）；修复点 ③ 豁免清单落表 |
| 2026-08-05 | active | active | agent | 波 2 装配注入收口完成：装配面/字段接口化、check-acp-imports.sh 验收门挂接、peri-lsp 依赖移除、测试签名跟进 |
| 2026-08-05 | active | active | agent | 波 3 ci-gate：三个旧 check 脚本合并为 check-layer-imports.sh + import-exemptions.conf（八边 × 双模式，Seam 3 完整版），lefthook/CI 各挂单一条目，豁免活性 26/26 交叉验证 |
| 2026-08-05 | active | active | agent | 波 4 v1-retire：v1 ExecutorEvent 中间态退役（StreamingContext v2 化 / StreamingEventBridge 与 inject_event_handler 删除 / subagent Started·Stopped 改 v2 事件构造 + 协议化同步映射直发 / guard 合并），v1 兼容层仅留协议序列化面最小映射（peri-acp-types），ARC-EVENT-001 更新 |
| 2026-08-22 | active | active | agent | 当前代码复核：L5 executor 已迁 Agent；BgRegistryEvent 明确为 private/capability projection；LlmRetrying 与 fatal prompt error 分界落地；Langfuse 实现归 Controller、ACP forwarder 仅接线；历史裁定不回写，依赖门收紧仍待专项验收 |
