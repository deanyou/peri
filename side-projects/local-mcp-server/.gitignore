# 本目录是独立 workspace：产物、运行日志与证据都不进入版本库。

# Rust 构建产物
/target

# 门禁/证据脚本在项目根直接写出的日志与退出码表。
# 正式证据一律落 .peri/adlc/tasks/<task>/artifacts/，根目录只允许临时输出，
# 因此这里忽略掉，避免包根被运行痕迹污染（WP-010 F5）。
/*.log
/exit-codes.tsv

# 运行时产物目录：本产品把工具产物与后台任务日志写在工作区内的 .local-mcp/
# （当工作区就是本目录时会出现在这里）。
/.local-mcp/

# 编辑器与系统文件
.DS_Store
*.swp
