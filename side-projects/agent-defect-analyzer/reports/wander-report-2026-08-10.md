# Wander 闲逛报告 v3 — 2026-08-10（08-02 ~ 08-07 样本）

> 历史快照：本报告保留人工抽样的研究上下文，不是当前数据结论。原一次性导出入口已移除；请按项目 README 和 `ANALYSIS.md` 使用后续统一 CLI 重建证据。
> 方法：`scripts/wander.ts`（seed=20260810）从 124 个长会话（message_count≥100、created ≥ 2026-08-01）随机抽取 5 个 → 导出精简轨迹 → 5 个独立 subagent 各审计一个 → 交叉汇总。
> 抽样：`019fd9ec` Fenix 压测优化 / `019fcc0e` e2e 测试修复 / `019fcb61` 消息区吸底修复 / `019fd635` FenixAgent 服务器部署 / `019fc059` 弹窗修复+devflow 收尾
> 数据口径：8-01 起仅顶层 tool_calls 格式，导出已归一化，无双写伪影；5 文件全部 <1800 行可整读；已排除双写/参数名归一伪影

## 一、五个会话速览

| 会话 | 日期 | 任务 | 消息数 | 总体评价 | 浪费比例估算 |
|------|------|------|-------:|----------|-------------|
| 019fd9ec | 08-07 | Fenix 认证热路径压测优化 | 142 | 方法学强（对照实验、严格验证），但凭据用错引发 7 轮混乱、深挖 18+ 轮未闭环 | 18-22% |
| 019fcc0e | 08-04 | e2e 测试修复 workflow（25 测试 5 失败） | 297 | 最终交付成功，但静态推测 60+ 轮、提交 scope 越界 | 30-35% |
| 019fcb61 | 08-04 | 消息区吸底粘性修复 | 146 | 节奏好、一次修复到位；commit 超时+fmt 未预检拖尾 | 12-15% |
| 019fd635 | 08-06 | FenixAgent 部署到 pazhou 服务器 | 142 | 意图误解整条路线作废 + 10 分钟前台静默阻塞，用户喊停 | 40-50% |
| 019fc059 | 08-02 | 弹窗修复 + devflow 收尾 | 113 | 纪律最佳；浪费主要来自工具层（Grep path 失效、commit 超时） | 8-10% |

## 二、跨会话共性模式

### 🔴 P0-1 Grep/Glob path 参数失效（本轮 2/5，历史 5/5 → 持续未修，工具层实锤）

- 证据：019fc059（#1→#3）`path=popup_overlay.rs` 返回 `submit_consumer_test.rs`/`mouse_router.rs`；（#16→#17）`path=…/kit/popups` 返回 `langfuse-client/src/error_test.rs`；（#18→#19）`path=…/rewind_popup.rs` 返回 `TUI-STYLE.md`、`spec/`。命中内容与各自 pattern 吻合，排除错配伪影；019fcb61（#41）`path=仓库根` 前 20 条命中全在无关 crate。
- 影响：噪音污染上下文；agent 把工具 bug 合理化为"pattern 太宽"（019fc059 #18），可能基于跨 crate 噪音得出错误结论。
- 修复方向（沿用 + 升级）：① 结果回显 `searched_in`/命中文件数（"searched 1,204 files repo-wide"）；② 命中全在请求 path 之外 → 醒目 WARNING；③ path 不可达硬报错；④ 复现单文件 pattern 含 `\|` 场景。

### 🔴 P0-2 连续无产出推测不停 + 系统提醒被忽略（本轮 3/5，历史 3/5 → 持续）

- 证据：019fcc0e 静态推测 60+ 轮、4 次 reminder 被选择性忽略（#30/#92/#107/#230），agent 自己说"需要运行时证据"却继续读代码 28 轮（#93→#120）；019fd9ec 深挖 18+ 轮，3 次提醒后继续（#77/#82/#116/#128），用户需求 100rps 早在 195rps 时已满足；019fd635 两次提醒被覆盖且两次都错（#44/#103，覆盖后 3 轮内被用户证伪）。
- 影响：每会话 20-60 轮空转 ≈ 15-25 分钟；是当前最大的单点浪费。
- 修复方向：reminder 升级为 AskUserQuestion 卡片（选项：派 explorer / 重跑拿运行时证据 / 收敛）；提醒附"已空转轮数 + 最近 3 个无结果查询 + 距用户显式需求的差距"；对"每轮有新实验数据"的假设驱动深挖与"无产出空转"区别对待。

### 🟠 P1-1 窄输出过滤器吞错误 → 全量重跑（本轮 2/5，历史 3/5 → 持续）

- 证据：019fc059 `cargo test | tail -8` 吞掉 `error[E0308]` 头（#30→#33），重跑一次全量编译；019fcc0e e2e 重跑 `grep -E "✓|×|passed"` 吞掉完整 dump（#248→#255 agent 才意识到被截断）。
- 修复方向：Bash 结果对 cargo/rustc 自动提取 `error[E…]` 块；e2e 失败重跑默认保留完整 dump；对 `tail -N`（N<30）管道警告。

### 🟠 P1-2 `git commit` 默认 15s 超时被杀 + 提交前无 fmt 预检（本轮 2/5，历史后台 15s 超时的 commit 变体）

- 证据：019fcb61 前 6 个 cargo 命令都显式设 timeout，唯独 #131 `git commit` 用默认 15s 被 lefthook 打断；019fc059 #101 同场景，超时错误提示"避免扫描大目录"与 commit 无关，agent 事前已知 lefthook 存在仍不预防。
- 连锁：fmt 未预检 → hook 失败 → 手动修 3 处（019fcb61 #137-140）或 cargo fmt 后提交再补测（019fc059 #104-106）。
- 修复方向：① 对 `git commit`/`cargo`/`npm` 等慢命令按类别预设更长 timeout；② 检测 `.lefthook.yml`/`pre-commit` 时主动提示 timeout≥120s；③ commit 失败后解析 hook 错误给出可操作建议（"fmt --check 失败，先跑 cargo fmt"）；④ 超时错误按命令类型定制提示文本。

### 🟠 P1-3 重复 Read 同一文件/同区间（本轮 4/5，历史 2/5 → 回升）

- 证据：019fcc0e `rewind_candidates.rs` 3 次（#89/#98/#259）、`rewind-v2.test.ts` ≥3 次；019fd635 `sync-to-server.sh` 2 次、`docker-compose.stress.yml` 2 次、`Dockerfile` 3 次；019fc059 全读后又 offset 重读（#7+#9）；019fcb61 scroll.rs 分 8 次局部读、区间重叠 ~700 行（#9→#30）。
- 修复方向：Read 返回"共 N 行已返回 1-M"；同文件 mtime 未变时重复 Read 提示"内容未变，可滚动回看或 grep 定位"。

### 🟠 P1-4 后台任务生命周期失控（本轮 3/5，历史 2/5 → 升级）

- 证据：019fd635 复现命令尾部 `&` + 前台 600s 超时 → 10 分钟静默阻塞（#113-114），孤儿任务不清理、后续部署被旧任务排队拖慢（#130 耗时 91s）；019fcc0e 用户关闭后台任务后不确认即重启（#206-207），且两个后台调用返回相同 task_id（#120-122，疑似工具 bug）；019fd9ec 两次 ssh 超时转后台丢输出、同模式重复犯（#79/#123）。
- 修复方向：① 前台长命令看门狗——超时 50% 且无输出时弹"转后台/终止换策略"；② 启动新后台任务时列出存活旧任务；③ 后台完成通知附"如何探测远端进程"指引；④ 排查相同 task_id 问题。

### 🟠 P1-5 用户意图误解/显式约束被自行解释（本轮 2/5，新浮现）

- 证据：019fd635 "传递必要镜像"被解读为"本地构建应用镜像"，12 轮 + 2 次失败构建后用户澄清（#23→#51）；019fd9ec 用户"只能走公网接口"约束被自行解释为"容器内 localhost 也行"（#53→#94 用户主动发问才补公网压测）。
- 修复方向：启动预估 >5 分钟的长耗时任务前，若指令含可替换领域名词 → 意图确认卡片；用户显式约束抽取为任务 checklist，执行计划与约束对照。

## 三、单会话独有发现

| 发现 | 会话 | 说明 |
|------|------|------|
| 🔴 mixed 压测 1300 errors 仅凭"数字吻合"（6525×20%≈1300）归因 404，未验证错误码分布 | 019fd9ec | 结论建立在运气上；rate-limit 实际存在（#120 才发现），可能混入 429 |
| 🔴 凭据归属的压缩失真（工具层） | 019fd9ec | 摘要把 API key 描述为"system API key (RCS_API_KEYS)"，名称与归属矛盾；agent 用错 key 401 引发 7 轮混乱 |
| 🔴 `git add -u` 提交 pre-existing 无关改动（12 files/311 insertions） | 019fcc0e | 用户明确说"提交暂存区"，agent 说"检查一下"但没检查；会话结束时未处理（灾难级个案） |
| 🟠 workflow verify 阶段不跑 e2e，误报"全部成功" | 019fcc0e | 用户基于误报 20 分钟，2/5 仍失败才暴露 |
| 🟠 复现命令选错构建目标 | 019fd635 | 失败步骤在 `build:web`，复现却用 `--target deps`（不含该步骤），什么也验证不了 |
| 🟠 `docker pull` 明知 arm64/amd64 差异仍不带 `--platform`，拉了两次废镜像 | 019fd635 | 同一会话内已建立架构知识（#21），执行未落实（#52→#62） |
| 🟠 压缩摘要状态重建成本高 | 019fc059 | 压缩后 3 段自述推理才理清"还剩什么没做"（#50）；注入文件快照无新鲜度标记 |
| 🟡 测试计数跨轮不一致（695→694）未被察觉 | 019fc059 | 新增测试后反而少 1，算术上不可能；疑似伪影但 agent 未对比 |
| 🟡 错分支提交未核实 | 019fc059 | 功能提交落在 main（上轮在 feature/20260801），仅报告未核实 |
| 🟡 线上 docker-compose.yml `sed -i` 无备份 | 019fd9ec | 恢复后验证过（无事故），但无回滚点 |
| 🟡 `bun build --target=bun` 多余（bun 可直接跑 .ts） | 019fd9ec | 2 次多余构建 + 额外传输 |
| 🟡 Grep pattern 含 `\"` 转义错误必然失败 | 019fcc0e | 基于错误结果推断"handler 在别处"，多绕 2 轮 |

## 四、与前轮对比（v2 截至 08-02，10 会话）

| 模式 | v2 | v3 | 结论 |
|------|----|----|------|
| Grep/Glob path 失效 | 5/5 | 2/5 | 持续未修，019fc059 为强证据（单文件 path 返回其他文件） |
| 后台任务 15s 默认超时 | 2/5 | 2/5（commit 变体）+ 3/5 生命周期失控 | 升级：出现孤儿任务、10 分钟静默阻塞、不确认重启 |
| 问用户太晚/推测不停 | 3/5 | 3/5 | 持续；reminder 被覆盖率 100%（5 次覆盖 4 次错） |
| 重复 Read | 2/5 | 4/5 | 回升，回到 v1 水平 |
| shell 管道吞输出 | 3/5 | 2/5 | 持续，形式变为 tail/grep 窄过滤 |
| git 危险操作 | 1/5 | 1/5 | 个案性质不同：v2 stash 丢工作，v3 是 add -u scope 越界 |
| subagent 结果不复用 | 3/5 | 0/5 | 消失（019fcc0e #239 派 explorer 一次命中根因，正面案例） |
| 新浮现：意图误解/约束被解释 | — | 2/5 | 新增 |
| 新浮现：提交前 fmt 未预检 | — | 2/5 | 新增，与 commit 超时同源（lefthook 契约未前置） |
| 新浮现：凭据压缩失真（工具层） | — | 1/5 | 新增，压缩保真问题 |

## 五、数据真实性说明

- **导出归一化**：019fcc0e #292 摘要只显示 `cargo fmt`，#296 文本证明同轮还执行了 `git add -u`+commit——同轮多工具调用可能被折叠，F3 调用序列无法 100% 还原；Read 结果摘要截断（只显示头部若干行），"agent 实际看到全文"为推断。
- **Grep path 失效**：019fc059 3/4 次命中文件全在请求 path 外且与 pattern 内容吻合，倾向真实工具 bug；但"path 被忽略 vs 导出错配"仍无法从数据侧区分，需复现实验确认（历史 v1/v2 已有 5/5 复现支持）。
- **时间估算**：各会话浪费比例为审计员推算（±10%），导出仅部分消息带时间戳。
- **#120 相同 task_id**：可能是归一化伪影也可能是真实 bug，无法区分。
- **019fc059 L3（695/694）**：算术矛盾无法解释，疑似导出伪影，**不要**据此断言"agent 漏了回归"；但未对比计数是事实。
- **MM 状态谜团**：`git status --short` 前导空格可能被归一化吃掉，agent 的困惑部分可能由导出格式造成。
- **commit message 追加 `Co-Authored-By: deepseek-v4-flash <deepseek-ai@claude-code-best.win>`**（019fcb61 #143）：来源与邮箱真实性无法从导出验证，若为模型幻觉则属误导性行为，证据不足仅提示。
- **超出导出范围**：019fc059 压缩覆盖了前半段 devflow 消息；019fd9ec reminder 引用的"9 轮推测"在压缩前，无法审计。
